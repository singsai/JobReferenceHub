var User = require('/database-helpers.js');

/*

  Important functionalities.
  
  addUser = function(object) {
    // Adds a user to the database. The object defines the user's properties.
  }
  
    Example:  addUser({
                name: 'Bob',
                age: 15,
                dog: 'Joey'
              });






*/